---
course: "The Mastery"
module: "Module 1: Cursor Setup and Configuration for AI-Directed Building"
lesson: "Module 1: Cursor Setup and Configuration for AI-Directed Building — Exam"
type: "course_quiz"
post_id: 107131651
space_id: 24191170
source: "https://the-faction.mn.co/posts/107131651"
updated: "2026-09-10T20:04:42Z"
---

# Module 1: Cursor Setup and Configuration for AI-Directed Building — Exam

> Exam for **Module 1: Cursor Setup and Configuration for AI-Directed Building** (The Mastery) — 25 questions, pass mark 80%. Correct answers are marked ✅ (read from each question's own post record; no quiz attempt was started to get them).

### Q1. A builder installs Cursor and starts working by dragging single files into the editor as needed. Their AI responses feel generic and ignore the rest of the project. What went wrong?

- **A.** They opened loose files instead of the project folder, so the AI never saw the project structure and has no context beyond each single file  ✅
- **B.** They skipped the account sign-in step, since AI responses only become project-aware after the account has been verified through email first
- **C.** They used the wrong file extensions, since Cursor's AI can only build context from files whose extensions are registered in its settings panel
- **D.** They installed the wrong edition of Cursor, since only the team edition includes the project awareness that single-file work requires here

> **Answer:** A

### Q2. A builder wants to understand Cursor's interface before their first real project. Which four zones does this module say they should identify?

- **A.** The debugger, the profiler, the memory monitor, and the network inspector, since performance analysis is where builders spend their time
- **B.** The editor, the file explorer sidebar, the chat panel, and the terminal, with most builder time spent in the chat panel and the editor  ✅
- **C.** The marketplace, the theme picker, the keybinding editor, and the account page, since configuration surfaces are the core of the interface
- **D.** The preview pane, the deploy panel, the analytics view, and the billing page, since shipping and measuring are the zones that matter most

> **Answer:** B

### Q3. A builder never checks which AI model Cursor is using and notices output quality varies unpredictably between projects. What does this module say about model selection?

- **A.** Model selection is locked per account, so varying quality between projects must come from the projects themselves rather than the model
- **B.** Cursor always uses one fixed model, so quality variation is caused entirely by instruction wording and never by which model is running
- **C.** Which model is selected silently determines the quality of everything you get back, so knowing and choosing your model is a core setup habit  ✅
- **D.** Models only affect response speed, not quality, so the builder should check their internet connection rather than the model selection

> **Answer:** C

### Q4. A builder is starting work on a client's codebase containing sensitive business logic. Which setup decision does this module say they should make deliberately?

- **A.** Disable the terminal, since client work requires preventing any commands from running against the client's code during the engagement
- **B.** Turn off codebase indexing, since indexing client code is what exposes it and disabling the index is the long-standard confidentiality measure
- **C.** Install a second copy of Cursor, since client work must always run in a separate application instance to keep the projects isolated fully
- **D.** Enable privacy mode so the client's code is not retained for training, making the data handling call on purpose rather than by accident  ✅

> **Answer:** D

### Q5. A builder memorized every menu path in Cursor, then a major update moved and renamed several panels. This module recommends a different anchor. What is it?

- **A.** Principles that survive versions: know your model, control your context, keep indexing on for real projects, and check settings after updates  ✅
- **B.** Turning off updates permanently, since a frozen version preserves the memorized menu paths and stability matters more than new features
- **C.** Switching tools whenever the interface changes, since an updated interface means the learning investment is lost and starting fresh is faster
- **D.** Waiting six months after each release, since menu layouts stabilize over time and late adoption preserves the value of memorized paths

> **Answer:** A

### Q6. A builder asks why codebase indexing matters before starting heavy work on a real project. What does indexing actually do?

- **A.** It compresses the project to save disk space, since indexed files are stored in a compact format that speeds up opening large folders
- **B.** It builds a searchable map of the project so the AI can find relevant code when you reference the project instead of only open files  ✅
- **C.** It backs the project up to the cloud, since the index is primarily a recovery mechanism protecting builders from losing local work
- **D.** It scans for security vulnerabilities, since the index is a static analysis report the AI consults before agreeing to make any changes

> **Answer:** B

### Q7. During first launch, Cursor offers to import VS Code settings and extensions. Per this module's decision framework, who should accept the import?

- **A.** Nobody, since imported settings conflict with Cursor's AI features and every builder should start clean regardless of their background
- **B.** Everyone, since the import is required for extensions to work at all and skipping it permanently disables the extension marketplace
- **C.** Only team accounts, since settings import is a collaboration feature designed to synchronize configurations across an organization
- **D.** Builders who already work in VS Code daily, while builders new to these environments start clean so their defaults stay predictable  ✅

> **Answer:** D

### Q8. A builder cannot remember where a feature moved after a Cursor update. Per this module, what is the fastest reliable way to find it?

- **A.** Reinstall the previous version, since rolling back is quicker than relearning and the old layout is the one that their muscle memory expects
- **B.** Search their notes from the course, since recorded menu paths from training are the authoritative reference for the current interface
- **C.** Ask Cursor itself where the feature lives in the current version, since the AI plus the official docs beat memorized paths that go stale  ✅
- **D.** Browse every menu systematically, since a full manual sweep of the interface is the only method that cannot be wrong about locations

> **Answer:** C

### Q9. A builder wonders how Cursor fundamentally differs from a traditional editor in their hands. Which framing matches this module?

- **A.** Cursor is a directing environment: you describe what you want, the AI produces it, and you review and steer, so setup serves that loop  ✅
- **B.** Cursor is a typing accelerator: it completes the code you were already writing faster, so setup is all about maximizing keystroke speed
- **C.** Cursor is a hosting platform: it runs your production apps, so setup is primarily about configuring servers and deployment targets
- **D.** Cursor is a design tool: it turns mockups into layouts, so setup is about connecting your design files and asset libraries properly

> **Answer:** A

### Q10. A builder's new project needs a runtime (Node) they have never installed. Per this module's setup approach, what should they do?

- **A.** Choose a different project, since missing runtimes signal a project too advanced for a builder at the setup stage of this course
- **B.** Install every popular runtime preemptively, since a complete runtime library prevents this interruption on all future projects
- **C.** Ask in chat and let the AI direct them through installing it, since directing configuration works the same way as directing building  ✅
- **D.** Contact Cursor support, since runtime installation is an environment issue that the application vendor is fully responsible for solving

> **Answer:** C

### Q11. A builder asks when privacy mode is genuinely optional rather than important. Which situation does this module treat as acceptable for leaving it off?

- **A.** Any project under active deadline, since privacy mode adds processing overhead that slows responses when speed matters most to delivery
- **B.** Your own experiments where you have no confidentiality concerns, while client work and sensitive material get privacy mode turned on  ✅
- **C.** All client projects, since professional work benefits from training-data contribution and clients expect their code to improve the models
- **D.** Projects with fewer than fifty files, since small codebases carry no meaningful information and privacy settings only matter at scale

> **Answer:** B

### Q12. This module recommends confirming the integrated terminal works during setup by running one command. Why does the terminal matter to a builder who directs AI?

- **A.** The terminal is where code gets written, since directing AI happens through terminal commands rather than through the chat interface
- **B.** The terminal is decorative in Cursor, but confirming it works verifies the installation completed without corruption in other areas
- **C.** The terminal replaces the chat panel on larger projects, so builders must confirm it early before the chat panel stops being available
- **D.** The terminal is where things run, so verification, installs, and the outputs you feed back to the AI all depend on it working  ✅

> **Answer:** D

### Q13. A builder finishes configuring Cursor and asks what ongoing maintenance the setup needs. What does this module recommend?

- **A.** Spend two minutes confirming settings survived after major Cursor updates, since setup is a standing state to maintain, not a one-time task  ✅
- **B.** Nothing, since Cursor migrates every setting perfectly across all updates and post-update checks waste time better spent building
- **C.** Full reinstallation monthly, since accumulated configuration drift can only be cleared by wiping the application and starting over
- **D.** Export settings to paper, since printed configuration records are the only reliable defense against updates changing your environment

> **Answer:** A

### Q14. A builder asks how the strongest available model and a faster model should divide their work. Which division does this module recommend?

- **A.** Faster models for architecture and planning, strongest for small edits, since even simple changes deserve the most capable model available
- **B.** Strongest model for planning, architecture, and complex builds; a faster model is fine for small mechanical edits when speed matters more  ✅
- **C.** Always the fastest model, since response speed compounds across a session and model capability differences are too small to matter
- **D.** Alternate between them randomly, since varying the model prevents overfitting your directing style to any one model's preferences

> **Answer:** B

### Q15. A builder deciding between Cursor and Claude Code as their daily environment asks how this module frames the choice. Which framing matches?

- **A.** Claude Code replaces Cursor entirely, since terminal tools are the professional standard and visual environments are training wheels
- **B.** Cursor replaces Claude Code entirely, since visual environments made terminal tools obsolete for anyone directing AI to build today
- **C.** Cursor is a visual environment suited to builders who want to see everything, Claude Code excels at long autonomous runs, and many builders use both  ✅
- **D.** The two are identical in capability, so the choice is purely aesthetic and this module declines to describe any difference between them

> **Answer:** C

### Q16. On opening a client project for the first time, a builder wants the AI to establish context before any changes. Which first instruction matches this module's pattern?

- **A.** Analyze this codebase: what is this project, what does it need to run, and does anything look misconfigured, before touching anything  ✅
- **B.** Delete all comments and unused files, since cleaning the project first gives the AI a smaller and clearer codebase to work within
- **C.** Generate documentation for every file, since complete documentation must exist before the AI can safely analyze project structure
- **D.** Rewrite this project using current best practices, since modernization is the safest first step on any unfamiliar codebase you inherit

> **Answer:** A

### Q17. A builder asks what actually activates Cursor's AI features after installation. What does this module say?

- **A.** Writing a configuration file by hand, since AI activation requires a fully manually authored settings document at the project root first
- **B.** Purchasing a hardware key, since Cursor's AI features require a physical authentication device before any model access is granted
- **C.** Completing a tutorial project, since AI features unlock only after the builder demonstrates competence in the guided walkthrough
- **D.** Signing into a Cursor account, which activates the AI features, after which settings confirm which models are available and selected  ✅

> **Answer:** D

### Q18. A builder complains that Cursor's AI answers about their project ignore most of their actual code. Indexing is off. What is the connection?

- **A.** Indexing controls response length, so with it off the AI truncates answers before reaching the parts about the builder's actual code
- **B.** The AI cannot reference what it cannot find, so with no index the answers ignore the real codebase and drift toward generic responses  ✅
- **C.** Indexing is unrelated to AI answers, so the ignored code must be caused by file permissions blocking the AI from reading the project
- **D.** Indexing only affects search speed, so the AI sees everything either way and the ignored code reflects a model capability limit instead

> **Answer:** B

### Q19. A builder wants their setup instructions to follow the same pattern they will use for building. Which directing pattern does this module teach for setup?

- **A.** Apologize first, then ask permission, then wait, since polite framing is what determines the quality of configuration assistance
- **B.** Paste the entire settings file and ask what is wrong, since raw configuration dumps are the standard input for setup direction
- **C.** State your situation, state your goal, ask for the steps, since you direct configuration the same way you will direct building  ✅
- **D.** Describe your feelings about the tool honestly, since emotional context helps the AI calibrate its configuration recommendations

> **Answer:** C

### Q20. A builder asks why this module keeps saying Cursor updates frequently. What practical consequence does the update pace carry?

- **A.** Menu names and panels shift, so builders anchor on principles that survive releases rather than memorized interface spots  ✅
- **B.** Builders should avoid learning the tool deeply, since any interface knowledge will be obsolete before it pays for itself
- **C.** Builders should pin one version forever, since the update pace makes staying current impossible for anyone shipping client work
- **D.** Builders should file bug reports weekly, since frequent updates mean frequent regressions that the community must document

> **Answer:** A

### Q21. A builder using VS Code for years worries their existing themes and keybindings will be lost in Cursor. What does this module say?

- **A.** Keybindings transfer but themes cannot, since Cursor's rendering engine is incompatible with VS Code's theming system entirely
- **B.** Nothing transfers, since Cursor's architecture is unrelated to VS Code and every customization must be rebuilt from scratch
- **C.** Only paid themes transfer, since Cursor honors marketplace purchases but discards free customizations during the import process
- **D.** Cursor is a fork of VS Code, so themes, keybindings, and most extensions carry over and first launch can import them directly  ✅

> **Answer:** D

### Q22. Halfway through setup, a builder asks where their attention will actually go during real work. Which time split does this module describe?

- **A.** Mostly the settings panels, since AI-directed building is continuous configuration and the editor is only glanced at occasionally
- **B.** Mostly the chat panel and the editor, in that order, since directing happens in chat and reviewing what was produced happens in the editor  ✅
- **C.** Mostly the file explorer, since organizing folders is the core activity and the chat panel is a minor accessory to file management
- **D.** Evenly across all zones, since the interface is designed for equal rotation and any imbalance signals a workflow problem forming

> **Answer:** B

### Q23. A builder asks what makes settings like indexing, privacy mode, and model selection different from ordinary preferences. How does this module frame them?

- **A.** They are standing decisions that shape what the AI sees and how it responds, made once, improving every future request you send  ✅
- **B.** They are temporary toggles reset on every launch, so builders should not invest thought in decisions the application will forget
- **C.** They are cosmetic choices, similar to themes, that affect how the interface feels without changing what the AI actually produces
- **D.** They are advanced developer options, irrelevant to builders who direct AI, and best left untouched at whatever defaults shipped

> **Answer:** A

### Q24. A builder asks why the module insists on opening the project folder even for a quick one-file tweak in a real project. What is the reasoning?

- **A.** Single files load slower than folders, since Cursor's file handling is optimized for directories and penalizes individual file access
- **B.** Licensing requires it, since Cursor's terms of service prohibit editing files that were opened outside of a project folder context
- **C.** The AI needs the structure the folder provides, since even a small change lands better when the model can see what surrounds it  ✅
- **D.** Folders enable autosave, since files opened individually cannot be saved automatically and risk losing the builder's accepted changes

> **Answer:** C

### Q25. Looking across this whole module, what is the governing principle of Cursor setup and configuration for AI-directed building?

- **A.** Match a checklist exactly once, since setup is only a gate to pass through at installation and never revisit for the lifetime of the tool
- **B.** Minimize configuration time, since defaults are professionally chosen and any builder adjusting settings is procrastinating on the real work of building
- **C.** Maximize installed features, since the builders who enable the most options and extensions get the most capable AI responses on every project
- **D.** Configure so the AI sees what it needs and behaves how you decided: folder open, index on, model set, privacy chosen, principles over menus  ✅

> **Answer:** D


---
_Take it in the browser: https://the-faction.mn.co/posts/107131651_
